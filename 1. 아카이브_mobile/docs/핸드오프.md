# SJ 아카이브 — 작업 핸드오프 문서

> **이 문서의 목적**: 새 Claude 채팅(또는 다른 사람)이 이 파일 하나로 프로젝트의 현재 상태·맥락·다음 할 일을 빠르게 잡기 위함.
> 코드 구조 규칙은 [`CLAUDE.md`](../CLAUDE.md), 콘텐츠 생성은 [`지식배달_콘텐츠_가이드.md`](../지식배달_콘텐츠_가이드.md)를 함께 볼 것.
> **마지막 갱신: 2026-06-28**

---

## 1. 한 줄 요약

매일 아침 AI가 큐레이션하는 **이중 모드(직장인 / 수험생) 지식 서재 모바일 PWA**.
- **직장인 모드(PROFESSIONAL)**: 영어·시황·역사·고전·고사성어·인사이트 지식 배달
- **수험생 모드(EXAM_PREP)**: 오답 사진 분석·수능 영단어·한국사 배달, D-day·복습 관리

## 2. 라이브 / 배포

| 항목 | 값 |
|---|---|
| 운영 URL | https://sj-archive.fly.dev |
| 인프라 | Fly.io (영구 볼륨 `/data`, always-on) |
| 리포 | https://github.com/jjang4099-beep/Kim |
| 재배포 | `cd "1. 아카이브_mobile"` → `flyctl deploy --remote-only` |
| 빌드 | Dockerfile (node:20-alpine), `public/`만 복사 |

> ⚠️ `CLAUDE.md`에 "Render 무료 티어라 데이터 휘발" 경고가 남아있는데 **이미 Fly.io 영구 볼륨으로 이전 완료**됨(문서가 옛 상태). 저장 데이터는 재배포에도 보존됨.
> ⚠️ `flyctl deploy` 첫 시도가 토큰 검증 타임아웃으로 가끔 실패함 → **그냥 한 번 더 실행**하면 됨(코드 문제 아님).

## 3. 구조 핵심 (자세한 건 CLAUDE.md)

```
1. 아카이브_mobile/public/
├── index_mobile.html     # 앱 진입점, JS/CSS는 ?v=N 캐시버스팅 (수정 시 버전 ↑ 필수)
├── js/  core → home → feed/library/manage/add → app_exam → pwa
│   ├── core.js           # 전역 state·유틸·Mob={} 선언
│   ├── home.js           # 홈·공통카드·상세·모드전환(setMode)·검색
│   ├── feed.js           # 지식 배달
│   ├── library.js        # 내 서재
│   ├── manage.js         # 통계·복습·퀴즈
│   ├── add.js            # 추가 모달
│   └── app_exam.js       # 수험생 모드 (ExamMob 네임스페이스)
├── css/ style_mobile.css (현재 v83) · exam.css (현재 v9)
├── data/
│   ├── archive.db        # SQLite(sql.js) 런타임 — git 미추적, 직접 편집 금지
│   ├── knowledge_db/     # 직장인 시드 (영어·중국어·역사)
│   ├── work_db/          # 직장인 신규 시드 (고전·고사성어·인사이트)
│   └── exam_db/          # 수험생 시드 (영단어·한국사)
└── server.js             # Express API + SQLite + 배달 생성 로직
```

**반드시 지킬 규칙 (위반 시 버그):**
- 전역 함수/변수 새로 만들지 말 것 → `Mob`/`ExamMob` 메서드, `state` 객체에만.
- `fetch()` 직접 호출 금지 → `fetchJSON()`.
- **모드 격리**: 모든 items 조회/저장에 `mode` 파라미터 필수. 직장인↔수험생 콘텐츠는 폴더·테이블까지 완전 분리.
- CSS 색상 하드코딩 금지 → CSS 변수(`--bg`, `--text-1`, `--border`, `--title-ink` 등).
- JS/CSS 수정하면 `index_mobile.html`의 `?v=N` 올릴 것.

## 4. 콘텐츠 생성 (매일 추가)

- 단일 기준 문서: [`지식배달_콘텐츠_가이드.md`](../지식배달_콘텐츠_가이드.md) — 타입별 복붙 프롬프트 8종 + 다음 id 표.
- 작업 전 항상 `npm run scan-ids`로 현재 마지막 id 확인.
- **고사성어는 `idioms_and_quotes`(IQ_, 폐기) 쓰지 말 것 → `work_db/idiom_cards`(IDC_)** 형식 사용.
- 저장 위치: 직장인=`knowledge_db`/`work_db`, 수험생=`exam_db`. 서버가 폴더 전체 .json을 읽어 INSERT OR IGNORE 적재.

### 현재 마지막 ID (2026-06-28 기준)

| 타입 | 마지막 ID |
|---|---|
| english_theme_packs | PACK_EN_032 |
| english_expressions | EN_475 |
| chinese_expressions | ZH_127 |
| history_facts | HI_107 |
| classic_quotes | CLQ_076 |
| idiom_cards | IDC_062 |
| daily_insights | INS_032 |
| exam_vocab_themes | EV_PACK_018 |
| exam_vocab_words | EV_090 |
| exam_history_items | EH_072 |

### 06-28 추가된 파일 목록
- `knowledge_db/batch_13_mixed.json` — PACK_EN_029(이견·갈등 테마팩) + EN_366~385 + ZH_118~127
- `knowledge_db/batch_14_reallife_en.json` — PACK_EN_030(실생활 영어 테마팩) + EN_386~415
- `knowledge_db/batch_15_mideu_en.json` — PACK_EN_031(미드 표현 테마팩) + EN_416~445
- `knowledge_db/batch_16_everyday_en.json` — PACK_EN_032(직장인 일상 영어 테마팩) + EN_446~475
- `work_db/classic_quotes_08.json` — CLQ_067~076 (세네카 스토아 5편 + 괴테 5편)
- `work_db/idiom_cards_07.json` — IDC_053~062 (기사회생·삼고초려·천재일우·배수진 등)
- `work_db/daily_insights_04.json` — INS_023~032 (근본귀인오류·NVC·죄수의딜레마·계획오류 등)

## 5. 디자인 시스템 (v82 PARTHENON SANCTUARY)

컨셉: **파르테논 신전의 고결한 미학 + AI 자동화 큐레이션**.
- 배경 파피루스 베이지 `#F4F0EA`, 타이틀 미드나잇 잉크 `#030712`(`--title-ink`).
- **파스텔 음각 뱃지**: `.arch-badge` + `.arch-en/his/quo/idm/mkt/yt/other` (Cinzel serif + 대리석 text-shadow).
- 홈 탭바: 영문 세리프 + 활성탭 1px 만년필 언더라인.
- 서재: 2열 대리석 격자 서고(장서 표지), 카드 클릭 → **팝업 모달**(`Mob.openDetailModal`).
- 수험생 홈: `THE SCHOLAR'S SANCTUARY` 대들보 + 사틴 골드 D-day + `blind-inscription`(터치 정답 노출) + `출제자의 눈 · INSIGHT` 영역.

## 6. 작업 이력 (최근 → 과거, why 중심)

| 날짜 | 커밋 | 무엇을 / 왜 |
|---|---|---|
| 06-28 | — | **콘텐츠 대량 추가 세션** — JSON 시드 파일만 작성, 코드 무수정 |
| 06-27 | `db39177` | 중국어 20·한국사 20 콘텐츠 추가 (폐기 타입 IQ는 제외) |
| 06-27 | `bef6e43` | **서재 카드 → 팝업 모달**(좁은 인라인 펼침이 갑갑 + 원문확인 에러). **원문링크 버그 수정**(source가 `daily-feed` 같은 내부 태그일 때 잘못 링크→에러). **모드 전환 캐시**(전환 시 빈화면 깜빡임 제거, 재방문 즉시 복원) |
| 06-25 | `0ae9e92` | 홈·서재 PARTHENON 리디자인(위 5번) |
| 06-25 | `2f02c91` | **모드 누수 차단**: 직장인 배달 생성이 수험생 오답을 재료로 흡수하던 버그 — `generateDailyDelivery` 쿼리에 `mode=PROFESSIONAL` 필터 + 카드에 mode 명시 |
| 06-24 | `e8949d5` | 콘텐츠 가이드 전면 갱신 + `scan-ids` 스크립트 |
| 06-24 | `c33757f` | 직장인 배달에 고전(LIBER)·인사이트·고사성어 `work_db` 연결 |
| 06-22 | `f0ff737` | 라이트 모드 검은 배경 누수 차단(시스템 다크 + 앱 라이트 조합) |
| 06-22 | `f46b563` | 서재 YouTube 썸네일 카드 + 별도 카테고리 |
| 06-22 | `bacf452` | 오답 문제집 PDF 내보내기 + 홈 영단어 더보기 + 앱 시작 로고 스플래시 |
| 06-21 | `171a2e1` 외 | 수험생 모드 재설계(과외 선생님 분석·차분한 학구 디자인·점증 D-day·다중 사진 첨부) |

## 7. 열린 결정 / 다음 후보

- **모드 전환 UX 미결**: 캐시(빠른 복원)는 넣었으나, 전환을 "페르소나(가끔)"로 볼지 "이중생활(수시 토글)"로 볼지 미정 → 실제 사용 후 결정 예정. 결정에 따라 전환 버튼 위치/마찰 설계가 갈림.
- **콜드스타트(빈손 출발)**: 신규 사용자는 초기 며칠 화면이 비어 보임. 배달 생성이 "최근 내 아이템"을 재료로 쓰는 닭-달걀 구조. → 신규용 시드 콘텐츠 + 온보딩 후보.
- **기능 발견성**: 스와이프 삭제 등 숨은 제스처/기능이 많음. → 발견 보조 후보.
- **아이디어 백로그**: 소셜방·지식마켓·개인화 추천·서재 보고서 등(별도 메모).

## 8. 새 채팅에게 한 마디

이 리포는 성숙한 코드베이스다. **기능을 새로 만들기 전에 `CLAUDE.md` 체크리스트를 먼저 보고, 비슷한 함수가 이미 있는지 `Mob`/`ExamMob`에서 확인할 것.** 모드 격리와 `?v=N` 캐시버스팅을 빠뜨리면 조용히 깨진다.

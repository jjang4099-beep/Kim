# 의존성
node_modules/
*/node_modules/

# 환경변수 (API 키 절대 올리면 안 됨)
.env
.env.local
.env.production
.env.development
# .env.example 은 샘플 파일 — 실제 값 없으므로 커밋 허용
!.env.example

# 로그
*.log
server.log
server_fresh.log

# 임시 파일
temp_*.jpeg
temp_*.jpg
temp_*.png
*.tmp

# 업로드된 파일 (용량 큼)
public/uploads/
*/public/uploads/

# 압축 파일
*.zip

# OS 파일
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
*.swp
